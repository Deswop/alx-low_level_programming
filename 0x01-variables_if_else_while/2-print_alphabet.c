#include <stdlib.h>
#include <stdio.h>

/**
 * main - printing the english alphabets
 *
 * Description: using the putchar to print english alphabets
 * Return: 0
 */
int main(void)
{
	char ch;
	for (ch = 'a' ; ch <= 'z' ; char)
	{
		putchar(ch);
	}
	putchar("\n");
	return (0);
}
